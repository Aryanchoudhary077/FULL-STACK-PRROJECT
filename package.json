{
  "name": "to-do-list",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix todolist",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix todolist\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
